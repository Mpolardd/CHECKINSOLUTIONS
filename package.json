{
  "name": "checkinsolutions-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx prisma generate --schema=backend/prisma/schema.prisma && cd web && npm install && npm run build",
    "start": "node backend/src/server.js",
    "test": "node backend/test/security_and_remediation.test.js && node scratch/production_readiness_e2e.test.js"
  },
  "dependencies": {
    "@prisma/client": "^6.16.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "prisma": "^6.16.0"
  }
}
